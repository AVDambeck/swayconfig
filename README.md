# swayconfig
my sway config. Vimish keybindings with lots of modes. also red and black colors

# copy.sh
copies the dotfiles to their locations

# compare.sh
compares your current yay -Q against packages.txt

# bar.sh
the command I use for swaybar
